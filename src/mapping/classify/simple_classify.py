from ...IOUtil import result_dir, Print
import json
import os

def load_ground_truth():
    path = os.path.join()

if __name__ == "__main__":
    base_dir = os.path.join(result_dir, '360/mapping/classify')
    truth_map, entities = load_ground_truth(os.path.join(base_dir, ))